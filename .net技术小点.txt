1.各类集合与泛型，反射。
2.委托事件task，action，线程，泛型委托。
3.集合类型的泛型化，线程安全否，实现机理（物理存储），基本操作的时空复杂度。
4.操作的多线程同步控制。

5.剩下几个点，wcf，web api，web service，soap，xml，http等一系列的演进路径。还有分布式系统。
6.缓存是与空间换时间的策略，关注点是缓存何时更新，何时触发，至于怎么跟新，更新算法已经回答了。消息队列是为了解耦，将调用者与被调用者解耦，引入一个第三方（软件领域常见手法），将需要执行的调用让消息队列存储起来，这样调用方可以不必等待调用结果，被调用方即使挂了也诶关系，消息队列里存在内容，也给被调用方以缓冲。